import {describe, it, expect} from 'bun:test';
import {CodeTable, JumpTable} from '../src';

describe('tables', () => {
  it('jumptable', () => {
    //
  });

  it('packed jumptable', () => {
    //
  });

  it('codetable', () => {
    //
  });
});
