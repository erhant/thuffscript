import {describe, test, expect} from 'bun:test';
import {CodeTable, JumpTable, PackedJumpTable} from '../src';

describe.todo('tables', () => {
  test('jumptable', () => {
    //
  });

  test('packed jumptable', () => {
    //
  });

  test('codetable', () => {
    //
  });
});
