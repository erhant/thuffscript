import {Macro} from './macro';

/**
 * Main macro.
 */
class Main extends Macro {
  constructor() {
    super('MAIN');
  }

  /** Compiles & returns the Huff source code. */
  compile(): string {
    return '';
  }
}
